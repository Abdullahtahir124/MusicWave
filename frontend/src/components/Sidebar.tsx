/**
 * Sidebar — legacy component kept for compatibility.
 * The main navigation sidebar is now rendered inline inside App.tsx Dashboard.
 * This file is retained so any future import doesn't break compilation.
 */
export function Sidebar(_props: { open?: boolean; onClose?: () => void }) {
  return null;
}
